services:
  postgres:
    image: postgres:17
    container_name: marysold_postgres
    restart: always
    environment:
      POSTGRES_USER: jimbo
      POSTGRES_PASSWORD: 3ntr4d4
      POSTGRES_DB: marysold
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
